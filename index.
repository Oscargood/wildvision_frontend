<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="icon" href="data:,">
    <meta charset="UTF-8">
    <title>Wildvision</title>
    <!-- Leaflet CSS -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.7.1/dist/leaflet.css" />
    <link rel="stylesheet" href="styles.css" />
</head>
<body>
    <!-- Main container with adjustable background -->
    <div id="container">
        <!-- Buttons above the map -->
        <div id="top-buttons">
            <!-- Existing Info Button -->
            <button id="infoButton">Info</button>
            <!-- Existing Static Text Box -->
            <div id="static_text_box">General animal behaviour notes</div>
        </div>
        <!-- Map Section -->
        <div id="map"></div>
        <!-- Day and Time Selection Display -->
        <div id="day-time-selection">
            Selected: <span id="day-time-text">Today Morning</span>
        </div>
        <!-- Controls Section -->
        <div id="controls">
            <!-- Combined Date and Time Slider Container -->
            <div id="date-time-slider-container">
                <label for="DateTimeSlider">Date and Time: <span id="DateTimeSliderLabel">Date and Time</span></label><br>
                <input type="range" id="DateTimeSlider" min="0" step="1">
                <button id="DateTimePlayPause">Play</button>
            </div>
            <!-- Layer Toggles -->
<div id="layer-toggles">
    <label class="layer-label">
        <input type="radio" name="layer-toggle" id="toggleAnimal" class="layer-button" checked>
        Animal Behaviour
    </label>
    <label class="layer-label">
        <input type="radio" name="layer-toggle" id="toggleTemp" class="layer-button">
        Temperature
    </label>
    <label class="layer-label">
        <input type="radio" name="layer-toggle" id="toggleRain" class="layer-button">
        Rain
    </label>
    <label class="layer-label">
        <input type="radio" name="layer-toggle" id="toggleWind" class="layer-button">
        Wind Speed
    </label>
    <label class="layer-label">
        <input type="radio" name="layer-toggle" id="toggleCloud" class="layer-button">
        Cloud Cover
    </label>
</div>

        </div>
    </div>
    <!-- Modal Popup -->
    <div id="popupModal" class="modal">
        <!-- Modal content -->
        <div class="modal-content">
            <span class="close">&times;</span>
            <!-- Add your modal content here -->
            <p>Welcome to Wildvision! Use the sliders and checkboxes to explore the data layers.</p>
        </div>
    </div>
    <!-- Leaflet JS -->
    <script src="https://unpkg.com/leaflet@1.7.1/dist/leaflet.js"></script>
    <script src="./script.js"></script>
</body>
</html>
