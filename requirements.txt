Flask==2.3.2              # Adjust to your Flask version
gunicorn==20.1.0          # WSGI server for running Flask
Flask-Cors==3.0.10        # For handling CORS (if needed)
Flask-SQLAlchemy==2.5.1   # For database interaction (if needed)
Flask-Migrate==3.1.0      # For handling database migrations (if needed)
requests==2.28.1           # If making HTTP requests
